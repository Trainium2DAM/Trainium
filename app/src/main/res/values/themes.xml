<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Se cambia el parent a DayNight para que el sistema no fuerce el modo claro al iniciar -->
    <style name="Theme.Trainium" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Opcional: Color de la barra de estado para que coincida con la app -->
        <item name="android:statusBarColor">?android:attr/colorBackground</item>
        <item name="android:windowBackground">?android:attr/colorBackground</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>